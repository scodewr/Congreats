services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: congreats
      POSTGRES_USER: congreats
      POSTGRES_PASSWORD: congreats
    ports:
      - "5432:5432"
    volumes:
      - congreats_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U congreats"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  congreats_data:
